
env:
  global:
    - FOO=FUBU

  matrix:
    - M1=one

build:
  
  ci:
    - ps -eax
    - ls -al

  post_ci:
#    - docker build -t=shippabledocker/sample_node-beta .
 #   - docker push shippabledocker/sample_node-beta
     - docker build -t=vidyar/sample_node-beta .
     - docker push vidyar/sample_node-beta


integrations:
  hub:
    - integrationName: docker
      type: "docker"
      
