# Language setting
language: node_js

# Version number
node_js:
  - "0.12"

env:
  global:
    - FOO=FUBU

  matrix:
    - M1=one

build:
  
  ci:
    - ps -eax
    - ls -al

  post_ci:
    - docker build -t=shippabledocker/sample-node:$BRANCH.$BUILD_NUMBER .
    - docker push shippabledocker/sample-node:$BRANCH.$BUILD_NUMBER


integrations:
  hub:
    - integrationName: ship-docker
      type: docker
     
